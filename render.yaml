services:
  # Un solo servicio: Express sirve la API + el frontend compilado
  - type: web
    name: stock-analyst
    runtime: node
    buildCommand: npm install && cd server && npm install && cd .. && npm run build
    startCommand: node server/index.js
    plan: starter
    envVars:
      - key: NODE_ENV
        value: production
